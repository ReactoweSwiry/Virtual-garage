import { Surface, Text } from 'react-native-paper';

import { styles } from '@/lib';

export default function Profile() {
  return (
    <Surface style={styles.screen}>
      <Text variant="bodyMedium">Here we add a profile page</Text>
    </Surface>
  );
}
